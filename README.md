# Sample Searcher

Search audio samples using natural language. An Electron app that uses CLAP embeddings to find audio files by text descriptions.

## Features

- Analyze directories of audio files (wav, mp3, ogg, flac, m4a)
- Search samples using text queries
- Vector similarity search with sqlite-vec

## Setup

```bash
npm install
npm run dev
```
